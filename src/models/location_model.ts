// Country model
export interface Country {
  country_id: string;
  country_name: string;
}

// State model
export interface State {
  state_id: string;
  state_name: string;
}

// City model
export interface City {
  city_id: string;
  city_name: string;
}
