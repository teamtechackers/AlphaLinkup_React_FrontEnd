export interface FundSizeModel {
    id: number;
    investment_range: string;
    status: number;
}

export const FundSizeModelLabels = {
    ID: "id",
    INVESTMENT_RANGE: "investment_range",
    STATUS: "status",
    ACTIONS: "actions",
};
